#include "ui_input.h"
/* handleInput is not used in the current architecture:
   all input is handled inside print() via the ncurses loop
   and in main.c via scanf for E/S/G/Q commands.
   This stub satisfies the linker. */
void handleInput(void) {}